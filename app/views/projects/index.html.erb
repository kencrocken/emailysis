<h1>Current Projects:</h1>
<% if @projects.nil? %>
<h2>You do not have any projects.  Please create a new project.</h2>
<% else %>
  <% @projects.each do |project| %>
    <h3><%= link_to project.name, project_path(project.id) %> <small><%= "#{project.description}" %></small></h3>
  <% end %>
<% end %>
<h3><%= link_to 'Create New Project', new_project_path, class: "btn btn-large btn-primary" %></h3>
